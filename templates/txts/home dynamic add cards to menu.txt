<!--DYNAMIC CHEFS-->
    {% for person in persons %}
    <div class="carousel-item menu-item chef-item">
        <div class="card shadow-lg">
            <div class="img-wrapper">
                <img src="{{ person.photo }}" class="card-img-top" alt="Card image cap">
            </div>
            <div class="card-body">
                <h5 class="card-title">{{ person.name }}</h5>
                <p class="card-text">{{ person.intro }}</p>
                <div class="ratings">
                    {% for i in range({{ chefCard.rating }}) %}
                        <i class="fa-solid fa-star"></i>
                    {% endfor %}
                    <p> {{ reviews }} reviews</p>
                </div>
                <div class="other-details">
                    <a class="btn nav-link text-start" onclick="meal({{ chefCard.topMealId }})">{{
                        chefCard.topMeal.name }}<i class="fa-solid fa-arrow-right"></i></a>
                </div>
                <form action="/profile" method="GET">
                    <input type="hidden" value="{{ person.id }}" name="profileId">
                    <input type="submit" class="btn btn-outline-primary" value="More Details">
                </form>
                <a href="#" class="btn btn-primary">Send Request!</a>
            </div>
        </div>
    </div>
    {% endfor %}

    <!--DYNAMIC MEALS-->
    {% for meal in meals %}
     <div class="carousel-item menu-item meal-item">
        <div class="card  shadow-lg">
            <div class="img-wrapper"><img src="{{ meal.photo }}" class="card-img-top"
                    alt="Card image cap"></div>
            <div class="card-body">
                <h5 class="card-title">{{ meal.name }}</h5>
                <p class="card-text">{{ meal.intro }}</p>\
                <h6>Price</h6>
                        <p class="card-text">${{ meal.price }}</p>
                <div class="ratings">
                    {% for i in range({{ mealCard.rating }}) %}
                    <i class="fa-solid fa-star"></i>
                    {% endfor %}
                    <p> {{ reviews }} reviews</p>
                </div>
                </div>
                <div class="other-details">
                    <a class="btn nav-link text-start" onclick="profile({{ mealChefs['meal.id'].id }})">{{ mealChefs['meal.Id'].name }}<i
                            class="fa-solid fa-arrow-right"></i></a>
                </div>
                <form action="/profile" method="GET">
                    <input type="hidden" value="{{ meal.id }}" name="profileId">
                    <input type="submit" class="btn btn-outline-primary" value="More Details">
                </form>
                <a {% if isLoggedin %}
                        onclick="meal({{ meal.id }})"
                    {% else %}
                        onclick="login()"
                    {% endif %}" class="btn btn-primary">Order Now!</a>
            </div>
        </div>
    </div>
    {% endfor %}